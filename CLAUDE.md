# このリポジトリについて

Claude Code の再利用可能なスキル（`.claude/skills/`）を集めたリポジトリ。各スキルは `<name>/SKILL.md` を本体とし、必要に応じて `references/`（詳細資料）・`assets/`（雛形）・`scripts/`（実行コード）を持つ。

2026-08-26 に全スキルの監査を行い、重複・矛盾・配線不足を発見して修正した（`docs/skill-audit-2026-08-26.md`、未追跡ファイル）。以下の規約は、その監査で見つかった問題の再発を防ぐために書いている。

## コミット前に必ずスクラバを通す

```
python3 <どちらかの pre-commit-scrubber>/scripts/scan.py --repo <リポジトリルート>
```

`git add` の前ではなく **`commit` の前**に実行する（`scan.py` は `HEAD` との差分を見るため stage 済みでも検出できる）。検出はシークレット・PII・組織固有名の可能性があるものだけを機械的に拾う。誤検出（コミットSHA、コード例のプレースホルダなど）は reject してよいが、必ず中身を確認してから reject すること。詳細は `pre-commit-scrubber/SKILL.md`。

## 新しいスキルを孤島にしない

監査で最大の問題として見つかったのは「重複」ではなく「スキル同士が配線されていないこと」だった。参照が無いスキルは、隣接する内容を自前で再説明し始め、その再説明が時間とともに劣化・矛盾する。

**新規スキルを追加するコミットには、既存の関連スキルへの参照を最低1本含める。** 逆方向（既存スキルから新規スキルへ）も、既存スキル側の該当箇所が自然に触れられるなら追加する。参照は「読んでもよい」ではなく「ここから先はこちらが担当する」という**責務の境界線**として書く。

```markdown
実際に複数のAIエージェントを同時に走らせる場合、作業ツリーの分離やプロンプトの渡し方は `agent-parallelization` を参照する。
```

下限は最低1本だが、上限も意識する。**直接参照は2〜3本を目安にする。** それを超えて関連しそうなスキルが増えてきたら、参照ではなく本文中で名前だけ触れるに留める。参照は「呼び出したら必ず読みに行く」ものではなく必要になったときだけ辿るものだが、1回の呼び出しから2次・3次参照まで芋づる式に辿るとコンテキストを消費する。参照を増やす前に、そもそも別スキルに分けるべき内容が混ざっていないか（→「スキルを分けるか一つにまとめるか」）も確認する。

## description の書き方

- **いつ発火すべきか**を、ユーザーが実際に言いそうな文言を引用して書く（「〜したい」「〜して」）
- **何をするか**を、中心的な方法論や判断基準の名前とともに書く
- 発火を狙いすぎず外さない。似た文言で競合する既存スキルがあれば、判定条件（例：「ボトルネックが特定済みか」）を両方の description に明示して住み分ける。**片方だけに書いても衝突は解けない**（両側が判定条件を持つ必要がある）
- 不要な場面も書く（例：「すでに受入基準や非対象が明示された依頼には不要」）。これが無いと、隣接スキルとの境界がdescription上で曖昧なままになる

## 本文の構成

- 冒頭で中心命題を一つ立てる：「このスキルの前提は一つ：**...**」という定式（22スキル中14が採用。新規スキルもこれに揃える）。複数の主張を並べるのではなく、そこから他の判断が導ける一つの軸を選ぶ
- 見出しは、逐次手順なら `## Step N: ...`、独立した判断基準の集合なら `## <トピック>` の並び。どちらが合うかは内容の性質で決める
- SKILL.md は500行を目安に収める。ドメイン固有の詳細（大量の具体例、ツール固有の実装パターン）は `references/` に出し、本文には「読むタイミング」を書いた `## 参照ファイル` 節を置く（`code-reviewer`、`pull-request-composer` を参考にする）

## 境界を宣言する

自分のスキルが**扱わない**ことを明示する節を持つと、隣接スキルとの責務の重なりを未然に防げる。`database-performance/SKILL.md` の「適用範囲外」節が唯一かつ最良の実例（現状これしか無いので、今後増やしたい）。「〜は対象外。〜の相談は `<別スキル>` へ」という形。

## 正典を1つに決める

同じ判断基準や原則が複数のスキルで必要になったときは、**どちらか一方を正典にし、他方は参照に縮める。** 複製したまま両方を保守しようとすると、必ずどちらかが取り残されて劣化コピーになる（実例: `pull-request-composer/SKILL.md` の粒度要約リストが2項目欠落していた事故）。

正典の選び方の目安：
- トップレベルのスキルは `references/` 配下のファイルより発見されやすいので、内容の主題に最も近いトップレベルスキルを正典にする
- コミットメッセージに、なぜその側を正典にしたかを書く。後から見た人が判断を蒸し返さずに済む

## スキルを分けるか一つにまとめるか

キーワードや対象領域が似ていても、統合すべきとは限らない。分けるべきサインは次のどちらか：

- **人間の承認ゲートの性質が違う。** 一方は「別案でも進める確認」、もう一方は「NOなら止まる不可逆リスクのゲート」——このような違いがあるなら、統合すると承認が抱き合わせになり、片方の重みが薄れる（実例: `pull-request-composer` と `pre-commit-scrubber` を分離した判断）
- **前提とする実行環境が違う。** 片方は特定のプラットフォーム前提、もう片方はプラットフォーム非依存——統合すると、非依存側の対象範囲が意図せず狭まる

逆に、**同じ活動を別の観点から見ているだけ**なら、新しいトップレベルスキルにせず、既存スキルの `references/` に加える方が良いことが多い（実例: 「AI生成コードのレビュー観点」は独立スキルにせず `code-reviewer/references/ai-generated-code.md` にした——レビューという同じ活動の一観点だったため）。

## コミットの慣習

- 型・トレーラーの規約は `pull-request-composer/references/commit-convention.md` を正典とする
- `Co-Authored-By` などのトレーラーを付けるかどうかは、そのリポジトリの既存履歴に合わせる（`git log -1 --format='%B'` で確認）。自動付加を前提にしない
